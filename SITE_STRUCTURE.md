# صفحات وبسایت Bloomia Club

## ✅ ساختار صفحات پیاده‌شده

### 🏠 صفحه اصلی
- **مسیر:** `/`

### 📚 بخش کوچینگ
- **کوچینگ** (`/coaching`)
- **کوچینگ چیست؟** (`/coaching/what-is-coaching`)
- **جلسه معرفی رایگان** (`/coaching/free-intro-session`)

### 👥 بخش مربیان
- **تمام مربیان** (`/coaches`)
- **پروفایل مربی** (`/coaches/:id`)
  - **بیوگرافی و داستان** (`/coaches/:id/bio`)
  - **تخصص و روش** (`/coaches/:id/expertise`)
  - **بسته‌ها و قیمت‌ها** (`/coaches/:id/packages`)
  - **رویدادها** (`/coaches/:id/events`)
  - **نظرات و بررسی‌ها** (`/coaches/:id/reviews`)
  - **رزرو جلسه** (`/coaches/:id/book-session`)

### 🎪 بخش رویدادها
- **تمام رویدادها** (`/events`)
- **جزئیات رویداد** (`/events/:id`)

### 📄 صفحات دیگر
- **بلاگ** (`/blog`)
- **انجمن** (`/community`)
- **تماس با ما** (`/contact`)
- **پرسش‌های متکرر** (`/faq`)
- **حقوقی** (`/legal`)

---

## ⚙️ تغییرات ایجادشده

### 1️⃣ فایل‌های صفحات جدید
تمام صفحات در فولدر `src/views/` ایجادشده‌اند:
- **JavaScript Files:** 17 صفحه
- **CSS Files:** 17 فایل استایل

### 2️⃣ تغییرات Navigation
- ✅ بروزرسانی منوی دسکتاپ
- ✅ بروزرسانی منوی موبایل
- ✅ تمام لینک‌ها به صفحات جدید

### 3️⃣ تغییرات Footer
- ✅ بروزرسانی بخش خدمات
- ✅ بروزرسانی بخش پشتیبانی
- ✅ تمام لینک‌ها به صفحات درست

### 4️⃣ Routing Setup
- ✅ تمام Routes در `src/index.js` اضافهشده‌اند
- ✅ Dynamic Routes برای مربیان و رویدادها

---

## 🎨 طراحی و استایل
- تمام صفحات دارای **Header یکسان** (Navigation)
- تمام صفحات دارای **Footer یکسان**
- تمام صفحات دارای **Background یکسان** (#f8f9fa)
- تمام صفحات دارای **Typography یکسان**

---

## 📝 نکات مهم

### صفحات Skeleton
تمام صفحات دارای **ساختار اساسی** هستند و آماده برای اضافهکردن محتوا:
- Container structure
- Title و description
- Ready for content addition

### Dynamic Routes
صفحات مربیان و رویدادها می‌توانند **parameters قبول** کنند:
```javascript
/coaches/1        // صفحه مربی شماره 1
/coaches/2/bio    // بیوگرافی مربی شماره 2
/events/1         // جزئیات رویداد شماره 1
```

### SEO Ready
- تمام صفحات دارای **Helmet meta tags** هستند
- Title و description برای هر صفحه

---

## 🚀 بعدی چکار باید بشود؟

1. **محتوای صفحات** را اضافه کنید
2. **تصاویر و assets** را در جای مناسب قرار دهید
3. **API Integration** برای دریافت اطلاعات مربیان و رویدادها
4. **Forms** برای رزرو جلسه و تماس با ما
5. **Mobile Optimization** اگر نیاز باشد

---

**تاریخ ایجاد:** 31 دسامبر 2025
