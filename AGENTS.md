# Bloomia Club — Architecture, Engineering & Design Rules
# قوانین و اصول اساسی معماری، توسعه و طراحی وبسایت بلومیا کلاب (Next.js)

این سند مرجع رسمی، تخطی‌ناپذیر و دائمی برای تمام تغییرات، کدنویسی‌ها، کامپوننت‌ها و معماری وبسایت **بلومیا کلاب (Bloomia Club)** است. هر ایجنت، توسعه‌دهنده یا دستیار هوش مصنوعی موظف است در تمامی مراحل توسعه، دقیقاً بر اساس این چارچوب عمل کند.

---

## ۱. اصل اول: طراحی و پیاده‌سازی موبایل‌فرست قطعی (Strict Mobile-First)

* **اولویت شماره یک، کاربر موبایل است**: بیش از ۸۰٪ ترافیک بلومیا کاربران تلفن همراه هستند. تمام کامپوننت‌ها و صفحات باید **ابتدا برای صفحه موبایل (۳۶۰ تا ۴۲۰ پیکسل)** طراحی و کدنویسی شوند.
* **کلاس‌های پایه برای موبایل، بریک‌پوینت‌ها برای دسکتاپ**:
  - در Tailwind ابتدا استایل‌های بدون پیشوند برای موبایل تعریف می‌شوند: `w-full px-4 py-6 text-sm flex-col`.
  - سپس با بریک‌پوینت‌های `md:` و `lg:` و `xl:` استایل برای تبلت و دسکتاپ ارتقا می‌یابد: `md:flex-row md:px-8 lg:max-w-7xl`.
  - معکوس کردن این منطق (استایل دسکتاپ و بازنویسی با `max-md:`) اکیداً ممنوع است.
* **ارگونومی لمسی (Touch Targets)**:
  - اندازه تاچ تارگت هر دکمه، لینک یا المان قابل تعامل در موبایل حداقل `44x44px` باشد.
  - فاصله امن بین دکمه‌های مجاور حداقل `8px` باشد تا کلیک اشتباه رخ ندهد.
* **پیشگیری از اسکرول افقی ناخواسته**: هیچ کانتینری نباید عرض ثابت فراتر از عرض صفحه (`100vw`) داشته باشد. همه بخش‌ها باید ریسپانسیو و روان باشند.

---

## ۲. اصل دوم: معماری کاملاً ماژولار سکشن‌محور (Modular Section Architecture)

* **اصل تک‌مسئولیتی هر بخش (Single Responsibility Sections)**:
  - هر صفحه وبسایت از بالا به پایین به سکشن‌های کاملاً مستقل و کپسوله تفکیک می‌شود.
  - هیچ صفحه بزرگی نباید صدها سطر کد یکپارچه داشته باشد.
  - فایل صفحه در `app/(routes)/.../page.tsx` صرفاً نقش یک **Composer** (ترکیب‌کننده) دارد که سکشن‌ها را لود می‌کند و دیتای سروری را به آنها پاس می‌دهد.
* **ساختار استاندارد پوشه‌ها و فایل‌ها**:
  ```text
  components/
  ├── common/               # المان‌های مشترک (دکمه‌ها، بچ‌ها، کاردها، مودال‌ها، اینپوت‌ها)
  ├── layout/               # ساختارهای ثابت (Navbar، MobileDrawer، Footer)
  └── sections/             # سکشن‌های اختصاصی هر صفحه
      ├── home/
      │   ├── HeroSection.tsx
      │   ├── StatsSection.tsx
      │   ├── ServicesSection.tsx
      │   ├── CoachesCarouselSection.tsx
      │   ├── TestimonialsSection.tsx
      │   ├── LatestPostsSection.tsx
      │   ├── FaqSection.tsx
      │   └── CtaSection.tsx
      ├── coaches/
      │   ├── CoachHeroSection.tsx
      │   ├── CoachFiltersSection.tsx
      │   ├── CoachGridSection.tsx
      │   └── CoachFaqSection.tsx
      ├── blog/
      │   ├── BlogHeroSection.tsx
      │   ├── FeaturedPostSection.tsx
      │   └── BlogGridSection.tsx
      └── ...
  ```
* **قابلیت جابجایی و حذف مستقل**: حذف یا جابجایی یک سکشن نباید هیچ خطایی در سایر سکشن‌های صفحه ایجاد کند. هر سکشن اینترفیس شفاف (Props) و استایل مستقل خود را دارد.

---

## ۳. اصل سوم: استانداردهای دیزاین سطح‌جهانی، ذوق بصری و هنر بی‌نقص (Impeccable Craft & Taste)
*برگرفته از متدهای پیشرفته Impeccable.style و TasteSkill.dev برای جلوگیری از طراحی‌های مصنوعی و کلیشه‌ای هوش مصنوعی (Anti-Slop)*:

* **طراحی متعهد و با شخصیت، نه کلیشه‌های تکراری (No AI Slop)**:
  - استفاده از ترکیب‌های تکراری (کارت‌های هم‌اندازه با آیکون و تیتر تکراری، گرادیان‌های مصنوعی متن، گلس‌مورفیسم‌های بی‌هدف) ممنوع است.
  - هر صفحه باید ساختار ریتمیک و متنوع داشته باشد (تنوع در عرض، کنتراست فضاهای پر و خالی).
* **عمق و سایه‌های ارگانیک (Organic Depth)**:
  - سایه‌ها باید نرم، چندلایه و واقع‌گرایانه باشند (Soft multi-layered ambient shadows)، نه سایه‌های تیز، ضخیم یا رنگی اغراق‌آمیز.
* **فاصله‌گذاری و ریتم بصری (Spacing & Rhythm)**:
  - گروه‌بندی فشرده اجزای مرتبط و جداسازی بخش‌های غیرمرتبط با فضای تنفس سخاوتمندانه (Generous Whitespace).
  - فاصله بالای هر تیتر باید مشخصاً بیشتر از فاصله پایین تیتر تا متن توضیحی باشد.
* **ایالت‌های کامل کامپوننت‌ها (Component States)**:
  - تمام کامپوننت‌های تعاملی باید دارای ایالت‌های شفاف `hover`, `active`, `focus-visible`, `disabled`, `loading`, `empty` باشند.
  - حلقه‌های فوکوس دسترس‌پذیر (Focus Rings) منطبق با رنگ برند برای ناوبری کیبورد.
* **آیکون‌گرافی استاندارد**:
  - ممنوعیت مطلق استفاده از ایموجی به عنوان آیکون‌های رابط کاربری.
  - استفاده از سیستم یکدست آیکون برداری مدرن (مانند `lucide-react`) با وزن و ضخامت خطوط کاملاً هماهنگ.
* **جزئیات مرورگر و پرداخت نهایی (Browser Surfaces)**:
  - شخصی‌سازی رنگ انتخاب متن (`selection:bg-emerald-900 selection:text-emerald-100`).
  - اسکرول‌بارهای باریک و ظریف هماهنگ با تم.

---

## ۴. اصل چهارم: استفاده از به‌روزترین لایبرری‌ها و تکنولوژی‌های روز (Modern Tech Stack)

* **Next.js 15+ (App Router)** با قابلیت‌های کامل React 19، رندر سروری، Streaming و Server Actions.
* **Tailwind CSS مدرن**: استفاده از کدهای کم‌حجم، تمیز، ریسپانسیو و پشتیبانی از متغیرهای رنگی مدرن (oklch/oklab).
* **کامپوننت‌های پایه دسترسی‌پذیر (Radix UI / Headless Primitives)**: برای ساخت دیالوگ‌ها، دراور موبایل، منوهای کشویی، تب‌ها و آکاردئون‌های کاملاً بدون باگ، استاندارد کیبورد و دسترس‌پذیری WAI-ARIA.
* **میکرواینترکشن‌ها و انیمیشن با Framer Motion**: انیمیشن‌های ورود ملایم، اکاردئون‌های نرم، لود اسلایدرها و جابجایی بین صفحات با فیزیک طبیعی (Spring Transitions).
* **آیکون‌های مدرن**: پکیج سبک و تمیز `lucide-react`.
* **مدیریت دیتا**:
  - کلاینت رسمی و تایپ‌شده `@sanity/client` و `@sanity/image-url` برای وبلاگ.
  - کلاینت رسمی سروری `@supabase/supabase-js` برای مربیان و رزروها.

---

## ۵. اصل پنجم: سئو و جئو فوق‌حرفه‌ای (Deep SEO & GEO Standards)

* **رندر سروری و استاتیک کامل (SSR / SSG)**:
  - تمام محتوای متنی، مقالات، مشخصات کوچ‌ها و توضیحات خدمات باید در سرور رندر شوند تا در پاسخ اولیه HTML بدون نیاز به اجرای جاوااسکریپت کلاینت حضور داشته باشند.
* **سلسله‌مراتب معنایی HTML5**:
  - هر صفحه **دقیقاً یک تگ `<h1>`** دارد که کلیدواژه اصلی هدف را در بر می‌گیرد.
  - عناوین بخش‌ها با `<h2>` و زیربخش‌ها با `<h3>` مرتب می‌شوند.
  - استفاده دقیق از تگ‌های `<main>`, `<article>`, `<aside>`, `<section>`, `<nav>`, `<footer>`, `<header>`.
* **متاداده‌های داینامیک و OpenGraph خودکار**:
  - استفاده از تابع استاندارد `generateMetadata` در تمام صفحات پویا.
  - درج Canonical URL قطعی، تگ‌های `og:title`, `og:description`, `og:image`, `twitter:card`.
* **داده‌های ساختاریافته (JSON-LD Schemas)**:
  - تزریق خودکار اسکیماهای معتبر Schema.org:
    - صفحه اصلی: `Organization` و `WebSite`.
    - صفحه کوچ‌ها و پروفایل: `Person` با تخصص و مدارک کوچینگ ICF.
    - مقالات بلاگ: `Article` / `BlogPosting` به همراه نویسنده و تاریخ انتشار.
    - بخش سوالات متداول: `FAQPage`.
    - مسیرهای صفحات: `BreadcrumbList`.
* **بهینه‌سازی برای موتورهای هوش مصنوعی (GEO - Generative Engine Optimization)**:
  - ارائه پاسخ‌های مستقیم، کوتاه و مستند به پرسش‌های رایج در ابتدای بخش‌ها.
  - استفاده از لیست‌ها، جداول مقایسه‌ای و خلاصه نکات کلیدی تا موتورهای جستجوی AI بتوانند متن را به عنوان Featured Snippet یا AI Overview نقل قول کنند.

---

## ۶. اصل ششم: هویت بصری، دیزاین سیستم و تایپوگرافی فارسی (Brand & UI System)

* **زبان و چیدمان**: کل پروژه با مشخصه `dir="rtl"` و زبان `fa` پیاده‌سازی می‌شود.
* **تایپوگرافی بدون پرش (Zero Layout Shift)**:
  - استفاده از پکیج بهینه‌شده `next/font` برای لود استاندارد فونت فارسی (مانند **Vazirmatn** با تمام وزن‌ها).
  - استفاده از `font-display: swap` و کش بهینه.
* **پالت رنگی اصیل برند بلومیا**:
  - رنگ اصلی (Primary): سبز کله‌غازی عمیق و آرامش‌بخش `#1f3d3a`.
  - رنگ تأکیدی (Accent): رنگ‌های گرم و انرژی‌بخش خاکی و مرجانی `#BF4408` و `#E65103`.
  - رنگ پس‌زمینه و سطوح (Surface): ترکیب گرم و تمیز `#fafaf8` و `#ffffff`.
  - کنتراست متن‌ها مطابق استاندارد WCAG AA (حداقل ۴.۵ به ۱).

---

## ۷. اصل هفتم: مهاجرت فازبندی شده و بدون ریسک (Zero-Downtime Migration)

* پروژه قدیمی بلومیا کلاب تا زمان تکمیل، تست و تأیید نهایی دست‌نخورده باقی می‌ماند.
* توسعه در یک شاخه مستقل در گیت (`feature/nextjs-migration`) انجام می‌شود تا هیچ ریسکی متوجه محصول فعال نباشد.
